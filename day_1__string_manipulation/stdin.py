# Storing user input in variable 'fname'
fname = input("Enter your name: ")
# printing out the length of the string stored in var 'fname' using the built-in function 'len()'
print(len(fname))

# same thing could've been done in the following way (in one line):
print(len(input("Enter name: ")))